# Invalid baselines

The archived runs in this directory must not be used for effect claims.

- `auto-scholar-smoke-5`: generated by the old whole-query `AND` expression and affected by shared mock DOI Gold identities.
- `raw-arxiv-sample20`: generated by the same obsolete query path.

The replacement smoke is `artifacts/p1/auto-scholar-smoke/run-20260824`.

- `p2-live-wifi-deepseek-old`: generated before the current P2 manifest/query-id/final-output contract; paper provenance query IDs do not match the run manifest and cost/provider/final-selection fields are missing.
- `p2-self-test-final-old`: generated before the current two-iteration and `spar.final.v1` fixture contract.
- `p2-self-test-optimized-old`: generated before the current two-iteration and `spar.final.v1` fixture contract.

The current P2 replay baselines are `artifacts/p2/self-test` and `artifacts/p2/self-test-no-citation`.
